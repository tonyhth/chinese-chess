# v6.2 合入冲突预检报告（只读分析）

> 任务：洪涛 22:19 令，Luke 派 · 分析 2026-08-17 夜 · Alex
> 方法：`git merge-tree 40a01eb m1 v6.2`（纯内存三方合并推演，零工作区写入、零构建）+ 双向 diff --name-only 交集实核
> 锚：merge-base = 40a01eb · m1 tip = 867e230（+40 commits，92 文件）· v6.2 tip = 70fd378（+24 commits，61 文件）

## 一、结论（给 Luke 的定调）

**合入窗口工作量：小。文本冲突 6 文件 / 8 hunk，全部为"双侧同域改写"且模式高度收敛；无 src/ 引擎域文本冲突。预估解冲突 + 语义复核 0.5-1 小时（Tina 或 Cody 皆可执行，建议 Tina——测试基建域是她的清偿主场）。**

Tina 13 项手工清单和全量 5 的前置摸底：**不需延期，但全量 5 跑批必须在解冲突 + 双态编译门禁之后**（理由见 §四）。

## 二、冲突热点清单（6 文件 8 hunk，文本冲突）

### 域 1：测试基建（5 文件 6 hunk）——同一模式，解法统一

双侧都做了 TestL10nSupport.injectZhHans() 接线，**语义完全相同，仅书写形态不同**：

| 文件 | m1 侧（our） | v6.2 侧（their） | 建议 |
|---|---|---|---|
| B3S4MasterGameSearchTests | 单行 init 紧凑形 + Alex L2 注释 | `let savedLang` + 多行 init + "断言清偿 簇1"注释 | 取 **v6.2 形态**（Swift 风格更规范），注释合并 |
| D5I18nAuditVerificationTests | 同上模式 | 同上模式 | 同上 |
| I18nKeyCompletionTests | 同上模式 | 同上模式 | 同上 |
| TutorialI18nTests | 同上模式 | 同上模式 | 同上 |
| MasterGameFixTests | setUp 形态接线 | 同一逻辑，注释措辞异 | 二选一，零语义差 |

**关键事实**：TestL10nSupport.swift 本体双侧终态**逐字节一致**（`git show` diff 为空）——接线基础设施无冲突，仅消费侧书写形态打架。

### 域 2：V60Phase3Tests（1 hunk）——**唯一需要动脑的冲突**

困毙判负测试 fixture：m1 侧 = TestPieceFactory 工厂改写（沿用旧坐标）；v6.2 侧 = **fixture 坐标纠错**（红帅误放 row 9 黑方底线 → 重建红宫 row 0 正确局面）。
**解法 = 两者叠加**：以 v6.2 纠错后的坐标为准，用 TestPieceFactory 形态书写（`TestPieceFactory.makePiece(kind: .general, side: .red, position: Position(row: 0, col: 4))` 等 5 子）。这不是二选一，是合流。
附带：v6.2 侧同文件还有 TINA-P2-001 认输残留 KnownIssue 断言注记——保留 v6.2 版本即可（零 src/ 约束下暂标注，本就报 Luke/Ruby 跟进）。

### 域 3：DEVTEAM.md（1 文件 2 hunk）——文档措辞合并

m1 侧 Tina 踩坑注记（iOS 编译门禁块、project.yml git 跟踪更正）与 v6.2 侧 Eric 同主题精简版并存。解法：保留 m1 侧完整版（含 08-16 更正与错误跑法警示），v6.2 侧两行精简句是其子集，直接取 our 即可，无信息丢失。

## 三、自动合并成功但需语义复核的热点（changed-in-both 无冲突）

1. **project.yml + ChineseChess-iOS/project.yml**：双侧都改（m1 引擎侧 target 配置；v6.2 侧 MARKETING_VERSION 6.1.0→6.1.1）。合并后必查：版本号留 6.1.1 还是按合入打包 bump 6.2.0——**按 945dd80 commit 记录，此决策 Luke 已定"由洪涛定"，合入时别替洪涛定**。合并后 xcodegen regenerate 双工程再验证。
2. **ChineseChessApp.swift**：m1 加 `--nps-bench` CLI 分支（:44 区段）；v6.2 改 window frame minHeight + 删 engine fallback alert（:400 区段）。**分区不重叠，自动合并成立**；复核点 = v6.2 删除的 alert 依赖的 `engineFallbackMessage` 状态是否在 m1 引擎改动中有新消费方（预判无，cheapEval 分流不触 UI 层）。
3. **V40Phase1Tests**：m1 工厂改写 MoveHangFix 区段；v6.2 修的是**另一测试用例**的 Piece 构造（同行不同区，自动合并成立）。复核点 = 合并后该文件 `grep "Piece(kind:"` 应只剩 v6.2 侧有意保留的手写坐标（若 v6.2 新写块本可工厂化，属 P2 顺手项不阻塞）。
4. **AI/引擎源码域（src/ChineseChess）交集 = 仅 2 文件**：ChineseChessApp.swift（上述）+ Accessibility/.gitkeep（双侧均触碰，无实质冲突）。**P1-P4 引擎核心（SearchBoardV2 / Engine / MoveValidator 等 24 文件）与 v6.2 布局改动零交集——轴分离设计（v6.2 = 纯 UI/测试，零 src/ 引擎触碰的约束）兑现良好**。
5. v6.2 新增 19 个 Views 文件 + V62LayoutTests 等 8 个新测试文件：纯 their 侧新增，零冲突进树。

## 四、给 Tina 的前置摸底结论

- **13 项手工清单**：对象全部在 v6.2 侧 UI 域，与 m1 零交集，**不受合入影响，可按 v6.2 现状继续核对**；唯一交叉点 = 合并后须重验一次（内容不变但重跑确认零意外）。
- **全量 5 跑批时点**：必须在 ①6 文件解冲突完成 ②双工程 xcodegen regenerate ③macOS + iOS 双态编译门禁通过 之后。理由：33 文件工厂 sweep（m1 侧 38986fd）与 v6.2 断言清偿同文件混合后，编译期才能暴露遗漏 import / API 形态不匹配（如 v6.2 手写 id 形态与工厂默认 id 策略的边界）。
- **建议顺序**：解冲突（§二）→ regenerate + 双态编译（DEVTEAM iOS 门禁块照跑）→ 全量 5 → Tina 13 项手工复核收尾。

## 五、风险声明

- 本报告基于 merge-tree 推演，等价于 `git merge` 的文本层结果；语义层风险（编译/测试失败）只能由 §四 时序实测兜底——预检的意义是**把"惊喜"压缩到编译门禁可捕获的范围**，本报告显示该范围很小。
- m1 侧若在合入执行前再进 commit（P5 相关），冲突面可能微增，预估不改变域分布（引擎域 vs UI 域零交集的结构性结论不因 P5 文档/校准 commit 改变）。
