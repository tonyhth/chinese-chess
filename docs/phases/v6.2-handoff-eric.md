# v6.2 布局编码交接说明（Eric 接手）

> 写作：Alex（架构师）· 2026-08-15 深夜收口 · 对象：Eric（coder2，明日接手 v6.2 编码线）
> 用途：新 session 靠文件定位（S2 式开局）——本文档 + v1.2 设计文档双锚点
> 分支：`v6.2`（worktree `/tmp/v62-wt`，主树 ~/DevTeam/projects/chinese-chess 亦在此分支）

---

## 一、分支现状（截至本文档 commit）

| commit | 内容 | 状态 |
|--------|------|------|
| 5248975 | Info.plist `$(MARKETING_VERSION)` 机制化 + Settings 版本行（plist 在前 footer 在后） | ✅ 已验证 |
| de20403 | AboutView 双平台（版本/版权/隐私/反馈） | ✅ 编译绿（Luke worktree 验证） |
| （随本文档） | AboutView l10n 成员修复（Luke 顺手修，原 de20403 漏 `private let l10n = L10n.shared`）+ spike 重建件归档 | 本次收口 commit |
| — | **布局编码主体未开始**（ManagedSplit/两视图改造/T1-T10 均为 0 行） | Eric 从零起 |

## 二、布局编码任务定义（全部依据 macOS-layout-redesign.md v1.2，先读它）

**改造对象**：`MasterGameBrowserView.swift`（1471 行，macosPlayLayout）+ `PuzzleDemoView.swift`（599 行，macosLayout）——macOS 左右布局（棋盘左 + 右侧面板），iOS 路径不动（`#if os(macOS)` 保护）。

**核心组件（未写，从零）**：
1. `ManagedSplitView`（方案 B 定选）：HStack + 计算 width + 自绘 handle + DragGesture。**生产要点**（spike 已验证）：`@State` 中转拖拽中的 ratio（逐帧跟手）+ **onEnded 一次落盘 @AppStorage**（勿逐帧写 defaults）；NSCursor resizeLeftRight hover 光标（加分项）；双侧最小宽度钳制 `max(400, min(leftW, total-300))`；首帧从 @AppStorage 读 = 重启精确恢复（B 相对 HSplitView 的核心收益）。
   - 参考：`docs/spikes/managed-split-minispike.swift`（spike 全量代码 + 结论注释，可编译复跑：`swiftc -parse-as-library -o /tmp/spike-split docs/spikes/managed-split-minispike.swift`）
2. `MoveRecordPanel`：走法记录面板（两页面共用）——高亮当前步 + 点击跳转该局面（验收 P1-2）
3. 右侧面板容器（DemoSidePanel 意图）：点评常驻（切换走法同步更新 = P1-3）+ MoveRecordPanel + DemoControlBar（现有组件适配入面板）

**Sheet**：`.puzzles`/`.studyHub` minWidth 600→800（ChineseChessApp.swift；replay/settings/achievements/analysis 不动——Tina §五已逐 case 核实）。

**主窗口**：minHeight 750→600（ChineseChessApp.swift，验收 T8）。

## 三、测试要求（Tina 第 3 轮评审背书，验收硬门槛）

- **自动化 T1-T10**：新建测试文件（如 `src/ChineseChessTests/V62LayoutTests.swift`）——编译验证/组件存在性/#if os(macOS) 保护/sheet minWidth=800/minHeight=600。清单：**Tina 评审 `docs/reviews/macOS-layout-review-round3-tina.md` §三表**（不重抄）
- **半自动 M1-M10**：拖拽/持久化/点评同步/最小窗口等——Tina 执行（M4 拖拽手感兼兜 mini-spike 交互层确认）
- **iOS 回归 I1-I6**：模拟器（布局改动 iOS 侧零代码，回归确认即可）
- ⚠️ **Tina 工作树状态**：她有 7 个测试文件 M 态（断言清偿批次）——**不要 git add 她的文件**，你只管自己的新文件

## 四、验收标准（v1.2 §七，含 Tina 5 条 P1 补充）

见 `docs/design/macOS-layout-redesign.md` v1.2 §七（13 条 + 单源引用 Tina 清单）——每条标了 v1.2/Tina P1-N 溯源。

## 五、坑与注意事项

1. **中文路径 app bundle**（`中国象棋-v6.1.0.app` 等）在仓库根目录——别误删误提交，是打包产物
2. `docs/` 在 .gitignore 内（测试与设计文档需 `-f` 强加，或走例外规则——5248975 已有 -f 先例）
3. 构建验证：`xcodegen generate && xcodebuild -target ChineseChess -configuration Debug build`；测试**必须** `-skip-testing:EloBaselineTests`（DEVTEAM.md 铁律，Intel Mac 全量必超时）
4. AboutView 反馈邮箱常量空串 = 反馈行隐藏（待 Luke 提供，一词激活）
5. HSplitView 方案 A/B 已裁选定 B（mini-spike + Luke 确认）——**不要回头用 HSplitView 原生**，重启 50/50 是已否决路径

## 六、架构答疑（Eric 明日起）

v1.2 设计文档 + 本交接说明覆盖 90%；方案级分歧（如 ManagedSplit 需求变化、两页面结构差异处理）随时 sessions_send 我。
