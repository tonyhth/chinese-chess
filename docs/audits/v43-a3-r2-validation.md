# A3-r2 验证报告（lvl3 vs lvl4，15 局，v4.3 v1.2 规格）

> 测试：Tina · 2026-08-15 · v6.1.0 打包前置
> 运行：2026-08-15 12:10–13:08（墙钟 3490.9s，纯计算 3490.8s，无休眠空洞），pid 15168，Release binary，`--calibrate-native` 串行，`--save-moves` 逐局即时落盘，IDS_DEPTH_LOG=1
> 产物：日志 `/tmp/v43-a3-r2.log`（EXIT:0）· harness 报告 `calibration-results/native_lvl3_vs_lvl4_2026-08-15T05-08-33Z.txt` · 走法 `calibration-results/move-history/A34_game{1..15}_*` · OVERLAP dump `overlap-dumps/illegal-position-dump.log`（7 条）

---

## 0. 结论速览

| 项 | 结论 |
|---|---|
| 15/15 完整收尾 | ✅ EXIT:0，无崩溃/超时/trap |
| 污染剔除（三分支预案·分支 2） | ✅ g4、g13 剔除，有效 **13/15 ≥ 12 门限**，结果有效不重跑 |
| 倒挂哨兵（两阶段判定·第一阶段） | ✅ 有效局 BayesElo(lvl4 vs lvl3) = **+54 > 0**，无倒挂（旧 binary 08-14 同对阵 lvl4 视口 **-35**，换新后方向转正）；10 局检查点得分率 60%，未触发失败快检（<15%/>85%） |
| 区分度终局判据（v4.3 §5.2） | ❌ **未达**：有效局 +54 / 全 15 局 +70，均 < +80（三档处理的"不通过"档，距 +100 通过线更远） |
| OVERLAP 证据链 | ✅ 7 条 dump 逐条转录无遗漏，两形态分型（g4 防御命中型 / g13 实体腐败持续型），移交 M1 治本 |

**一句话**：数据面全部合格（完整性/有效性/无异常/证据链），区分度指标未过 v4.3 门槛——后续处置交 Luke 裁决（见 §7 Plan B 张力）。

---

## 1. 胜负统计双口径 + 先手方标注

配色：文件名红方在前，奇数局红=lvl3/黑=lvl4，偶数局互换（`SelfPlayRunner.run` swapSides，实测与 15 个文件命名一致）。

| 局 | 红 | 黑 | 结果 | 胜方级别 | 步数 | 终因 | 备注 |
|---|---|---|---|---|---|---|---|
| 1 | lvl3 | lvl4 | 红胜 | **lvl3** | 23 | normal（将死） | 冷门局，见 §4 depth0 交叉验证 |
| 2 | lvl4 | lvl3 | 和 | — | 500 | moveLimit | |
| 3 | lvl3 | lvl4 | 和 | — | 500 | moveLimit | |
| 4 | lvl4 | lvl3 | 和 | — | 500 | moveLimit | ❌ 剔除（OVERLAP） |
| 5 | lvl3 | lvl4 | 黑胜 | **lvl4** | 32 | normal | |
| 6 | lvl4 | lvl3 | 和 | — | 500 | moveLimit | |
| 7 | lvl3 | lvl4 | 黑胜 | **lvl4** | 68 | normal | |
| 8 | lvl4 | lvl3 | 红胜 | **lvl4** | 43 | normal | |
| 9 | lvl3 | lvl4 | 黑胜 | **lvl4** | 54 | normal | |
| 10 | lvl4 | lvl3 | 和 | — | 500 | moveLimit | |
| 11 | lvl3 | lvl4 | 和 | — | 500 | moveLimit | |
| 12 | lvl4 | lvl3 | 黑胜 | **lvl3** | 150 | **stalemate（困毙）** | 困毙方=红(lvl4)，红方无着且未被将 |
| 13 | lvl3 | lvl4 | 黑胜 | **lvl4** | 58 | normal（将死） | ❌ 剔除（OVERLAP，腐败局面参与终局） |
| 14 | lvl4 | lvl3 | 和 | — | 486 | repetition | |
| 15 | lvl3 | lvl4 | 和 | — | 500 | moveLimit | |

**双口径汇总**：

| 口径 | 红胜 | 黑胜 | 和 | lvl4 战绩 | lvl4 得分率 |
|---|---|---|---|---|---|
| 全 15 局 | 2 | 5 | 8 | 5胜2负8和 | 9/15 = **60.0%** |
| 有效 13 局（剔 g4/g13） | 2 | 4 | 7 | 4胜2负7和 | 7.5/13 = **57.7%** |

lvl4 的 2 负 = g1（lvl3 红先 23 步将死 lvl4）+ g12（lvl4 困毙）。

## 2. BayesElo（harness 同款公式复算）

公式：`Δ = -400·log10(1/p̂ - 1)`（`BayesElo.estimateDelta`，logistic，和棋 0.5 分）。

| 口径 | lvl4 视口（spec 语义） | 红方视口（harness 打印） |
|---|---|---|
| 全 15 局 | **+70**（p̂=0.600） | -70（harness 输出，交叉吻合 ✓） |
| **有效 13 局（主口径）** | **+54**（p̂=7.5/13=0.577） | -54 |

对照 v4.3 §5.2/5.3 三档：**+54 < +80 → ❌ 不通过档**（全 15 局 +70 同样 < +80，双口径结论一致，非边界样本争议）。

历史对照（lvl4 vs lvl3 视口）：08-14 00:42Z 旧 binary 10 局 = **-35**（倒挂方向）；08-14 13:17Z 仅 5 局（中断残局，不可比）。本轮换新 binary 后方向转正（-35 → +54），幅度不足。

## 3. move-history 15 局完整性

- **齐全性**：A34_game1–15 共 15 个新文件，mtime 12:11→13:08 单调有序，与日志局序一致；目录内混有旧文件（A23_×48、A45_×41、A34 旧时间戳），按 `A34_` 前缀 + 当日 mtime 分拣无混淆。
- **逐局步数与日志吻合**：文件名步数后缀 23/500/500/500/32/500/68/43/54/500/500/150/58/486/500 与日志逐局结果**全对全一致** ✓。
- **口径说明（非缺陷）**：文件无尾换行，`wc -l` 显示 N-1；字节交叉验证（如 500 步 = 500×5-1 = 2499B ✓，23 步 = 114B ✓），步数完整无丢失。
- 命名红方在前 + 奇偶交替，与 swapSides 代码行为一致（§1 配色表据此建立）。

## 4. [IDS] 深度分布

| 级别 | 条数 | 分布 | 备注 |
|---|---|---|---|
| lvl3（budget=3） | 2158 | depth3 ×2153（99.8%）+ depth0 ×5 | |
| lvl4（budget=6） | 2155 | **depth6 ×2090（97.0%）** + depth5 ×63（2.9%）+ depth0 ×2 | |

- **depth0 全部对应终局无着，交叉验证全对上**：lvl3 ×5 = g5/g7/g8/g9/g13 被 lvl4 将死；lvl4 ×2 = g1 被 lvl3 将死 + g12 困毙。7 个 depth0 与 7 个 decisive 局的终局状态一一吻合，无未知异常 ✓。
- **方法论注记**：[IDS] 仅记录走完 IDS 全程的搜索——lvl3 开局库命中（`mediumSearchScored` 无条件查库直接返回）与 lvl4 前 6 步库/CheckmateSearch 命中均不打日志，故行数 4313 < 总步数 4914（差 601 = 库/杀棋快路命中数）。不影响分布结论。
- **顺带观察（不构成本轮验收项）**：lvl4 搜索超 5000ms 预算 310/2155（14%），最大 23867ms（4.8×）；lvl3 0/2158 超时。属既有 TimeManager 弱点，M1 hotpath 治理范围，仅记录在案。

## 5. OVERLAP 专项（7 条 dump 逐条转录）

dump 全文在 `overlap-dumps/illegal-position-dump.log`；日志侧 [OVERLAP] 恰 7 行（log :1089, :3358-3364），两处一致 ✓。

### 形态 A：g4 ply#83 — 单次 pre，防御命中型（棋盘本体无重叠）

- **时间戳** 2026-08-15T04:26:45Z（12:26:45），game#4（红=lvl4 黑=lvl3），ply#83（1-based，probe histCount=82），turn=lvl4，preExecute。
- **detail**：`C1 from(1,4) 无棋子，但 move.piece=id0 chariot` ＋ `C2 move.captured=nil ≠ 棋盘 to 位=id=0 chariot`（to 位竟是走子自身——候选针对的源/目标格与活棋盘均已错位）。
- **overlap pairs：0**（dump 自注"无同格重叠——若本 dump 来自防御命中，检查重复 ID/其他非法态"）——即**候选被检查点拦下时，棋盘上没有实体同格重叠**。
- **棋盘快照关键字段**（16 子）：id0 红车在 (1,3)；而最近走法 #9 为 `id=0 chariot红 (5,4)→(1,4) captured[id=23 advisor]`——快照与走子史互相矛盾：**搜索所基于的候选棋盘与活棋盘已分叉**，stale candidate 特征坐实。
- **日志旁证**：OVERLAP 前后出现连续 lvl3 搜索夹住 turn=lvl4 的行棋轮（候选生成上下文错乱痕迹）。
- **后续**：无 post dump（执行后无重叠产生），对局正常走完。⚠️ **更正一处引用笔误**："43 步红胜"是 g8 的数据；g4 完赛形态 = **和棋 500 步（moveLimit）**，OVERLAP 后又正常行棋 417 步（12:27 落盘）。剔除理由不变（该步候选来源非法）。

### 形态 B：g13 ply#56–58 — 连续 3 ply × pre+post 双报，实体腐败持续型

时间窗 13:01:24.917→.980（63ms 内 6 条），game#13（红=lvl3 黑=lvl4）。

| # | ply | turn | 阶段 | 检出 | overlap pairs |
|---|---|---|---|---|---|
| 1 | 56 | lvl4 | pre | C3 重复位置(8,4)：id7 advisor红 ⚡ id6 advisor红（两红仕同格，ply≤55 已形成，本条首次检出） | 1（双红仕） |
| 2 | 56 | lvl4 | post | 执行 id19 horse (6,4)→(7,6) 后重叠仍在（该步未触碰 (8,4)） | 1（同上） |
| 3 | 57 | lvl3 | pre | C3 重叠持续 | 1 |
| 4 | 57 | lvl3 | post | 执行 id8 general 红 (9,5)→(9,4) 后重叠仍在 | 1 |
| 5 | 58 | lvl4 | pre | **C2 move.captured=id7 ≠ 棋盘 to 位=id6**（候选认为吃 id7、活棋盘 to 位是另一鬼影 id6）＋ C3 重叠仍在 | 1 |
| 6 | 58 | lvl4 | post | 执行 id30 soldier黑 (7,4)→(8,4) captured[id7]——落在仍被 id6 占据的格上，产生**新重叠对 id30 soldier ⚡ id6 advisor (8,4)**；棋子数 17→16 | 1（新对） |

- **终局耦合**：ply58 后红轮无合法应着且被将（黑卒 id30 于 (8,4) 直面红帅 (9,4)）→ 将死判黑胜 58 步 normal。**腐败局面直接参与终局判定**（鬼影仕干扰红方应将空间），g13 剔除的必要性坐实。
- **与形态 A 的分型差异**：g4 = 候选层拦截（0 实体重叠，单发，自愈）；g13 = **棋盘本体实体重叠持续 3 ply**＋捕获语义错乱（C2）＋重叠随走子再生。post 也报 = 执行后棋盘仍非法，与 g4"执行后干净"形态不同。
- **根因链（供 M1 治本）**：两形态同源——**搜索候选基于过期棋盘快照，搜索空间与活棋盘分叉**；M1 hotpath 重设计（棋盘表示/undo 路径）为治本 owner。本次为该根因第一次实战捕获，dump 已具备完整复现现场（棋盘快照＋最近 10 步含 captured＋调用栈）。

## 6. 无其他异常声明

- 崩溃：无（EXIT:0，log :4372）；超时中断：无；trap：无（全日志无 TRAP 行）。
- [OVERLAP] 全日志恰 7 行，与 dump 7 条一一对应，无遗漏；overlap-dumps/ 目录仅此 1 文件。
- 除 OVERLAP 外无 error/fatal/warn/assert 行（stalemate 仅作为对局终因词出现）。
- 运行进程已退出（pgrep 空）；15 局落盘时间线与墙钟 3490.9s 吻合。

## 7. 判定与移交

**v4.3 v1.2 体系内判定**：
1. 剔除协议（三分支预案分支 2）执行有效：13/15 ≥ 12，结果有效不重跑 ✅
2. 两阶段判定·倒挂哨兵：+54 > 0，无倒挂 ✅（相对旧 binary -35 为实质改善）
3. 区分度终局判据：**+54（有效）/ +70（全量）< +80 < +100，不通过** ❌

**⚠️ Plan B 张力（交 Luke 裁决）**：三档处理表中"+80 以下→Plan B"的三个升级选项（temp/maxDepth/搜索配置），v1.2 本身已是结构性升强（lvl4 `.hard` + depth6 + CheckmateSearch）后的产物——**出口选项已用尽**。可选方向（非测试职权，仅列）：a) 承接 M1 hotpath 治本后复验（OVERLAP 根因同属 M1 范围，一次治理两收）；b) 带 KnownIssue 打包 v6.1.0 并在说明中标注 A3 区分度现状；c) 再迭代一轮 v4.3 参数。判据与数据以上，决策在 Luke。

---
*复核基础：日志/dump/走法文件/harness 报告四源交叉；BayesElo 手工复算与 harness 输出吻合（+70/-70）；depth0×7 与 7 个 decisive 局终局一一对应；文件字节数与步数逐局核算。*
