# v6.3 Step 2 — E3 构造点收敛审计表（工单附录）

> 取证命令（验收 §3.3 口径）：
> `grep -rn "EmbeddedPikafishEngine(" --include="*.swift" 全仓`
> 执行 commit：本表随 E3 commit 入库（基线 1b70818，修复后见下表"处置结论"列）

## 一、应用代码构造点（全仓唯一合法 = EngineRouter）

| 文件 | 行号 | 构造形态 | 处置结论 |
|---|---|---|---|
| src/ChineseChess/AI/EngineRouter.swift | 42 | `acquireEmbeddedEngine()` 内 `EmbeddedPikafishEngine()` + start + 挂单例 | ✅ 合法唯一构造点（E3 统一入口；validate/switchEngineIfNeeded/engineFor 全部经此） |
| src/ChineseChess/AI/SelfPlayRunner.swift | 732（原） | 直构 + start，会话结束 shutdown | ✅ 已收敛：改经 `EngineRouter.shared.acquireEmbeddedEngine()`，结束改 `newGame()` 复位（不杀共享实例） |
| src/ChineseChess/AI/SelfPlayRunner.swift | 1037（原） | 同上 | ✅ 已收敛（同上） |
| src/ChineseChess/AI/SelfPlayRunner.swift | 1548（原） | CLI main 直构 | ✅ 已收敛（同上；CLI 进程内 Router 单例语义不变） |

## 二、分析/复盘/教练面/回放路径（单列条目）

| 路径 | 引擎获取方式 | 处置结论 |
|---|---|---|
| 分析面 PositionAnalyzer（evaluate/multiPV/quickClassify/analyzeMove） | `EngineRouter.shared.switchEngineIfNeeded()` → acquireEmbeddedEngine | ✅ bf215d9 起已走 Router；E5 后移除开关短路；不可用时返回 nil 交上层降级 |
| 复盘面 MasterGameCommentator / analyzeMoveLite | PositionAnalyzer（同上链路） | ✅ 无独立构造点 |
| 教练面 CoachExplainer / OpeningMoveEvaluator | PositionAnalyzer（同上链路） | ✅ 无独立构造点 |
| 回放/评测面 AnalysisViewModel / AssessmentSession / AchievementChecker | PositionAnalyzer 或 EngineRouter | ✅ 无独立构造点 |
| 对弈面 GameViewModel / PuzzleViewModel | `EngineRouter.engineFor/validate/switchEngineIfNeeded` | ✅ E5 后专业级恒 Pikafish，不可用 native 降级 |
| 自弈/校准面 SelfPlayRunner | （原直构 ×3） | ✅ 已收敛（见表一） |

## 三、测试代码构造点（测试 target，生命周期隔离）

B3S1OpeningCoachTests / PikafishCAPITests / CalibrationV3Tests / V34IntegrationTests / V34PhaseABCTest / V60Phase3Tests / V34PhaseABCTest 等测试文件内 `EmbeddedPikafishEngine()` 直构属**测试生命周期隔离**（quit/init 循环验证、未启动 guard 验证），不经 Router（MainActor 单例在测试宿主内状态跨用例泄漏）。处置：保留，不计入应用面"全仓唯一构造点"口径；验收 grep 命中即对号本表三节。

## 四、E5 开关全清取证

- `grep -rn "useEmbeddedEngine" --include="*.swift" --include="*.plist" src ChineseChess-iOS`
- 修复后命中（唯一合法残留）：`EngineConfigStore.migrateLegacyConfig` 的旧 key 清理字面量 `"chinesechess.useEmbeddedEngine"`（防脏 key 复活，属清理代码本体）及 Batch2P1Tests 对该清理的锢定用例。功能面（属性/Toggle/守卫/UI 绑定）零残留。
- 对账：ad9126b 守卫（开关守卫）随开关退场恒真移除，可用性守卫（isReady → native fallback）保留并加强（acquireEmbeddedEngine 单点）；58b384a iOS Toggle 入口删除；.active 预热线（两 App 的 switchEngineIfNeeded）无需适配自然成立（无条件确保引擎）。
