# C1 工单｜XCUITest 收编（v6.3 Step 3，Tina 执行 2026-08-29）

> 来源：v6.3-plan §2 Step 3 / scope-draft C1。Luke 派单：I4I6RegressionUITests 移出 gitignore 正式收编 + 六轮导航链跑通一次 + L3 纪律入验收项。

## 范围与执行

| # | 项 | 状态 |
|---|---|---|
| ① | target 工程接入（xcodegen）：ChineseChess-iOS/project.yml `ChineseChessUITests`（bundle.ui-testing, iOS）已含 UITests/ 目录，I4I6 文件在列；`xcodegen generate` + `build-for-testing`（iPhone 16 Pro 模拟器，无签名）TEST BUILD SUCCEEDED | ✅ |
| ② | .gitignore：`ChineseChess-iOS/` 整目录排除 → `/*` 化 + 白名单仅 `UITests/I4I6RegressionUITests.swift`（其余 UITests 文件维持忽略，等各自工单收编） | ✅ |
| ③ | 六轮导航链跑通一次：模拟器实跑（flock 互斥 + nohup + 固定日志 logs/step3/c1-ios-uitest.log） | 见报告 |
| ④ | 不扩新用例（Bug4*/Smoke* 不随批收编） | ✅ |

## 验收项（含 L3 真实性纪律，本工单起生效）

1. **L3 像素锚真实性纪律**：截图/像素断言锚**必含故障源真实组件**（不得只截容器/空白页）；I4=CommentaryOverlay 气泡、I5=走法记录面板、I6=DemoControlBar——三组件各自出现在锚图内方可判真。
2. **关键页面三图入档脚本化**：新 UI 测试引入关键页面时，须以脚本（非手工）产出 正常/空数据/错误态 三图入档 `docs/test/screenshots/`，图名带 suite 前缀；本批 I4I6 六锚图随 xcresult 附件留存。
3. **静默零跑门规**（沿用）：UI 测试批对账 "Executed N tests"，N=0 = 批次作废。
4. **互斥纪律**：UI 测试批同经 `~/DevTeam/.locks/xcode-tests.lock`（flock 排队），与正式批/r 系长跑互斥。

## SEL-1 同批对账

SelectionConsistencyTests 26 用例 + EngineTimeContractTests 3 用例：**已在库**（6f02c87 首入，054eb84 E5 维护），Step 2 终报批全绿复验（26/26 + 3/3），无新增变更——本批仅复验记录，不重复入库。
